import {when, given, then, binding} from 'cucumber-tsflow'

@binding()
export class CommonStepsDefination {
    @given('Open browser and go to login')
    public openBrowser(): void {
        
    }
}